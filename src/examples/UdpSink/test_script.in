#!/bin/bash -e

export APEX_UDP_SINK_HOST=127.0.0.1
export APEX_UDP_SINK_PORT=5560
#export APEX_UDP_SINK_CLIENTIP=`hostname`
#export APEX_UDP_SINK_CLIENTIP=localhost
export APEX_UDP_SINK_CLIENTIP=127.0.0.1

# start the server

# export APEX_SCREEN_OUTPUT=1
@CMAKE_CURRENT_BINARY_DIR@/../../udp_server/udp_server -s &

# start the client

unset APEX_SCREEN_OUTPUT
export APEX_UDP_SINK=1
@CMAKE_CURRENT_BINARY_DIR@/testBeacon

# kill the server, just in case

killall udp_server
